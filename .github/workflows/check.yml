# SPDX-License-Identifier: Apache-2.0

name: Check

on: [push, pull_request]

jobs:
  license:
    name: License
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: enarx/spdx@master
        with:
          licenses: Apache-2.0

  # commits:
  #   name: Commits
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #       with:
  #         fetch-depth: 0
  #     - uses: actions/setup-node@v3
  #     - run: npm install @commitlint/config-conventional
  #     - run: npx commitlint --extends @commitlint/config-conventional --from=${{ github.event.pull_request.base.sha }} --to=${{ github.sha }}
